<% @schedule.scheduled_events.each do |scheduled_event| %>
  <%= scheduled_event.event.description %> at <%= scheduled_event.time.localtime.strftime('%-l:%M %P') %> for <%= scheduled_event.duration %> minutes
<% end %>